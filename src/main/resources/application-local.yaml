spring:
  application:
    name: core-client-service
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:6432/devfheral
    username: app-msv-dev
    password: Franheral*12345!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
server:
  port: 8080
